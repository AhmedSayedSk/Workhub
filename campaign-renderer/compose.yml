services:
  campaign-renderer-worker:
    build: { context: .., dockerfile: campaign-renderer/Dockerfile }
    container_name: campaign-renderer-worker
    restart: unless-stopped
    env_file: .env
    tmpfs: [/tmp]
    networks: [campaign-renderer_net]
networks:
  campaign-renderer_net: {}
